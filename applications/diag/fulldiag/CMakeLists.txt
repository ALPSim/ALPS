if(LAPACK_FOUND)
  add_definitions(${LAPACK_DEFINITIONS})
  set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${LAPACK_LINKER_FLAGS}")
  add_library(fulldiag_impl STATIC factory.C)
  target_link_libraries(fulldiag_impl alps boost  ${LAPACK_LIBRARY} ${BLAS_LIBRARY})

  add_executable(fulldiag fulldiag.C)
  add_executable(fulldiag_evaluate fulldiag_evaluate.C)
  target_link_libraries(fulldiag fulldiag_impl alps boost ${LAPACK_LIBRARY} ${BLAS_LIBRARY})
  target_link_libraries(fulldiag_evaluate fulldiag_impl alps boost ${LAPACK_LIBRARY} ${BLAS_LIBRARY})
  install(TARGETS fulldiag fulldiag_evaluate RUNTIME DESTINATION bin)
else(LAPACK_FOUND)
  message(STATUS "fulldiag will not be built since lapack library has not been found")
endif(LAPACK_FOUND)
