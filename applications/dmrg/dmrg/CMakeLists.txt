if(LAPACK_FOUND)
  add_definitions(${LAPACK_DEFINITIONS} -DBIND_FORTRAN_LOWERCASE_UNDERSCORE)
  include_directories(${PROJECT_SOURCE_DIR}/applications/dmrg/dmrg)
  set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} ${LAPACK_LINKER_FLAGS}")
  add_executable(dmrg dmrg.C)
  add_executable(dmrg_complex dmrg_complex.C)
  target_link_libraries(dmrg alps boost ${LAPACK_LIBRARY} ${BLAS_LIBRARY})
  target_link_libraries(dmrg_complex alps boost ${LAPACK_LIBRARY} ${BLAS_LIBRARY})
  install(TARGETS dmrg dmrg_complex RUNTIME DESTINATION bin)
else(LAPACK_FOUND)
  message(STATUS "dmrg will not be built since lapack library is not found")
endif(LAPACK_FOUND)
