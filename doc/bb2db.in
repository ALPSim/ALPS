#!/bin/sh

export XML_CATALOG_FILES=catalog.xml

echo @XSLTPROC@ --xinclude -o "$2" @BOOST_DIR@/tools/boostbook/xsl/docbook.xsl "$1"
@XSLTPROC@ --xinclude -o "$2" @BOOST_DIR@/tools/boostbook/xsl/docbook.xsl "$1"

exit $?
